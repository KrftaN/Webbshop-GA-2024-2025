extends base

block content
    main
        form#sign-up-form(action="#" method="post")
            h2 Lägg till ny produkt
            input(type="text", placeholder="Namn", required)
            textarea(cols="20", rows="7", placeholder="Produkt Beskrivning")
            input(type="text", placeholder="Producent", required)
            input(type="text", placeholder="Pris", required)
            input#myFile(type="file", name="filename")
            input#myFile(type="file", name="filename")
            section
                input(type="checkbox", name="godkänner", required)
                label(for="godkänner") Kylvara
            input(type="submit", value="Lägg till")
